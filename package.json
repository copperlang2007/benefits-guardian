{
  "name": "benefits-guardian",
  "version": "0.1.0",
  "description": "Benefits optimization core for Social Security, Medicare, and VA benefit planning.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest --run",
    "ci": "npm run build && npm test"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^4.1.7"
  }
}
